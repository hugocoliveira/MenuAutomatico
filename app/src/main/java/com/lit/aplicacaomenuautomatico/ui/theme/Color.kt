package com.lit.aplicacaomenuautomatico.ui.theme

import androidx.compose.ui.graphics.Color

// ─── Paleta principal — azul #051FC7 ───────────────────────────────────────

/** Cor primária do app — azul escuro usado em TopAppBar, botões e destaques */
val Primary = Color(0xFF051FC7)

/** Container claro da cor primária — usado em chips e cards selecionados */
val PrimaryContainer = Color(0xFFD6DCFF)

/** Texto/ícone sobre fundos Primary */
val OnPrimary = Color(0xFFFFFFFF)

/** Texto/ícone sobre fundos PrimaryContainer */
val OnPrimaryContainer = Color(0xFF00105A)

// ─── Cor secundária ─────────────────────────────────────────────────────────

/** Azul médio — botões secundários e destaques complementares */
val Secondary = Color(0xFF3A4480)

/** Texto/ícone sobre fundos Secondary */
val OnSecondary = Color(0xFFFFFFFF)

// ─── Superfícies e fundos ───────────────────────────────────────────────────

/** Fundo geral das telas — branco levemente azulado para suavidade visual */
val Surface = Color(0xFFF8F9FF)

/** Variante de superfície — fundo de cards e itens de lista */
val SurfaceVariant = Color(0xFFE8EAFF)

/** Texto principal sobre superfícies claras */
val OnSurface = Color(0xFF0D0F1C)

/** Texto secundário/subtítulos sobre superfícies */
val OnSurfaceVariant = Color(0xFF44475A)

/** Fundo geral da tela */
val Background = Color(0xFFF8F9FF)

/** Texto sobre fundo geral */
val OnBackground = Color(0xFF0D0F1C)

// ─── Estados de erro ────────────────────────────────────────────────────────

/** Vermelho para mensagens e estados de erro */
val ErrorColor = Color(0xFFBA1A1A)

/** Texto/ícone sobre fundos de erro */
val OnError = Color(0xFFFFFFFF)

// ─── Bordas e divisores ─────────────────────────────────────────────────────

/** Cor de bordas, divisores e contornos */
val Outline = Color(0xFFC4C6D0)
